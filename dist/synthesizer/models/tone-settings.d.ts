export declare class ToneSettings {
    private attack;
    private sustain;
    private decay;
    constructor(attack?: number, sustain?: number, decay?: number, waveform?: string, volume?: number);
}
